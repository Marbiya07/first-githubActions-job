{
  "name": "first-githubactions-job",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "tests:headless": "npx playwright test",
    "tests:headed": "npx playwright test --headed",
    "test:specific:headless": "npx playwright test --grep @google",
    "test:specific:headed": "npx playwright test --headed --grep @google",
    "test:tag:headless": "npx playwright test --grep @wip",
    "test:tag:headed": "npx playwright test --headed --grep @custom-fixture",
    "test-smoke": "npx playwright test --grep @smoke",
    "report": "npx playwright show-report"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.31.1",
    "@types/node": "^25.6.0"
  },
  "dependencies": {
    "dotenv": "^17.4.2"
  }
}
